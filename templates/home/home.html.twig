{% extends 'base.html.twig' %}

{% block title %}Sortir.com{% endblock %}

{% block body %}
    <h1>Page d'accueil</h1>
    <a href="{{ path('app_sorties')}}">Liste des sorties</a>
    <p>Date du jour : {{ "now"|date("d/m/Y H:i") }}</p>
    {% if app.user %}
    <p>Participant : {{ app.user.firstname }} {{ app.user.name }}</p>
    {% endif %}
    <a href="{{ path('app_sortie_create') }}" class="btn btn-primary">Créer une sortie</a>

{% endblock %}
